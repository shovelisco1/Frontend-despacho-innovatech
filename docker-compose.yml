version: '3.8'

services:
  frontend:
    image: shovelisco/innovatech-frontend:latest
    container_name: web-frontend
    ports:
      - "80:8080"
    networks:
      - innovatech_front_net

networks:
  innovatech_front_net:
    name: network_frontend_innovatech